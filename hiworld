const name = 'Ruslan' 
const surname = 'Alyasin'
const tun = 'My name is:'

const getCarInfo = (name, surname) => {
  return tun + ' ' + name + ' ' + surname
}

display.log(getCarInfo(name, surname))
